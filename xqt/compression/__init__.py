"""Model-side compression: quantization and pruning.

Canonical packages:

- ``xqt.compression.quant``: quant policy, plan, quantizers, calibration
- ``xqt.compression.prune``: unstructured / structured / N:M / block sparse prune
"""
